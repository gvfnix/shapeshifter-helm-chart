configMaps:
  configs:
    config.yaml:
      global:
        log_level: debug
        nesting:
          level1.path: global.nesting.[level1.path]
          level2:
            path: global.nesting.leve2.path
    config.yml:
      global:
        log_level: debug
    config.ini:
      global:
        log_level: debug
    config.properties:
      global.log_level: debug
    config.json:
      global:
        log_level: debug

