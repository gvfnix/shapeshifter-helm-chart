configMaps:
  configs:
    config.yaml:
      global:
        audit:
          events:
          - ack
          - syn
        log_level: debug
    config.yml:
      global:
        log_level: debug
    config.ini:
      global:
        log_level: debug
    config.properties:
      global.log_level: debug
    config.json:
      global:
        log_level: debug
